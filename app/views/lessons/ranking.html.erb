<section class="title">
	<h1><%=@department.department_name %>の授業ランキング</h1>
</section>


<% @lessons.each do |l| %>
	<p>
		<%= l.lesson_name %>
	</p>
	<p>
		<% l.comments.each do |c| %>
		<% ca = c.average_star %>
		<%= ca %>
		<% end %>
	</p>
<% end %>

