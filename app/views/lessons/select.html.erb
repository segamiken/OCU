<section class="title">
	<h1>新規投稿</h1>
	<h3>投稿したい教科を選んでください！</h3>
</section>

<br>
<div>
	<%= link_to "授業がない場合はこちら", new_department_lessons_path(@department.id)%>
</div>


<%= will_paginate(@lessons,  :previous_label => ' &lt 前へ', :next_label => '次へ &gt', :renderer => WillPaginate::ActionView::LinkRenderer) %>

<% @lessons.each do |f| %>

<%= link_to lesson_comments_path(f.id) do %>
<div class="fuculty__top">
	<%= f.lesson_name %>
</div>
<% end %>

<% end %>

<%= will_paginate(@lessons,  :previous_label => ' &lt 前へ', :next_label => '次へ &gt', :renderer => WillPaginate::ActionView::LinkRenderer) %>

